import { SearchFiltrePipe } from './search-filtre.pipe';

describe('SearchFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
